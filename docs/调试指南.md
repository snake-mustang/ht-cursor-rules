# HT Cursor Rules - 调试指南

## 如何调试插件

### 方法一：使用 F5 快捷键（推荐）

1. **打开项目**
   - 在 VSCode 中打开 `ht-cursor-rules` 项目文件夹

2. **启动调试**
   - 按 `F5` 键（或者点击菜单 `运行 > 启动调试`）
   - 会自动编译代码并启动调试

3. **测试插件**
   - 会打开一个新的 VSCode 窗口（Extension Development Host）
   - 在新窗口中打开一个测试项目
   - 按 `Ctrl+Shift+P`（Windows）或 `Cmd+Shift+P`（Mac）
   - 输入 "HT" 或 "ht-rules"
   - 选择 "HT: 添加 Cursor 规则文件"
   - 测试两级选择功能

### 方法二：使用命令面板

1. **打开命令面板**
   - 按 `Ctrl+Shift+P`（Windows）或 `Cmd+Shift+P`（Mac）

2. **选择调试命令**
   - 输入 "Debug: Start Debugging"
   - 选择 "运行扩展" 配置

### 调试技巧

#### 1. 设置断点
- 在 `src/extension.ts` 中点击行号左侧添加断点
- 代码执行到断点时会暂停
- 可以查看变量值和调用堆栈

#### 2. 查看日志
- 在调试控制台查看 `console.log()` 输出
- 插件激活时会输出 "HT Cursor Rules 已激活"

#### 3. 重新加载
- 修改代码后，在 Extension Development Host 窗口中：
  - 按 `Ctrl+R`（Windows）或 `Cmd+R`（Mac）重新加载
  - 或者按 `Ctrl+Shift+F5`（Windows）或 `Cmd+Shift+F5`（Mac）重启调试

#### 4. 查看扩展输出
- 在 Extension Development Host 窗口
- 打开 "输出" 面板（视图 > 输出）
- 选择 "扩展主机" 频道

## 测试功能清单

### ✅ 基本功能测试

1. **命令注册**
   - [ ] 命令面板能找到 "HT: 添加 Cursor 规则文件"
   - [ ] 右键菜单能看到 "HT: 添加 Cursor 规则文件"

2. **类别选择**
   - [ ] 显示 6 个类别
   - [ ] 每个类别有图标和描述
   - [ ] 能正确选择类别

3. **规则选择**
   - [ ] 根据类别显示对应规则
   - [ ] 规则列表完整
   - [ ] 规则描述正确

4. **文件操作**
   - [ ] 创建新 .cursorrules 文件
   - [ ] 覆盖已有文件
   - [ ] 合并规则
   - [ ] 查看文件功能

### ✅ 各类别规则测试

#### Frontend Frameworks & Libraries (11 种)
- [ ] Angular Development
- [ ] Astro Best Practices
- [ ] Next.js Best Practices
- [ ] Next.js + shadcn/ui + Supabase
- [ ] Nuxt (Vue Best Practices)
- [ ] React Best Practices
- [ ] React + shadcn/ui + Vite
- [ ] React + shadcn/ui + Next.js
- [ ] SolidJS
- [ ] Svelte Development
- [ ] Vue.js Best Practices

#### Backend & Full-Stack (8 种)
- [ ] Python FastAPI
- [ ] Python Django
- [ ] TypeScript NestJS
- [ ] Node.js + MongoDB
- [ ] Go Backend
- [ ] Java SpringBoot
- [ ] Laravel PHP
- [ ] Ruby on Rails

#### Mobile Development (7 种)
- [ ] Flutter Clean Architecture
- [ ] Flutter Riverpod
- [ ] React Native Best Practices
- [ ] React Native + Expo Router
- [ ] SwiftUI
- [ ] Swift UIKit
- [ ] Android Jetpack Compose

#### Testing Frameworks (4 种)
- [ ] Cypress E2E Testing
- [ ] Playwright E2E Testing
- [ ] Jest Unit Testing
- [ ] Vitest Testing

#### Language-Specific (4 种)
- [ ] Python Best Practices
- [ ] TypeScript Code Quality
- [ ] C++ Programming Guidelines
- [ ] Rust Best Practices

#### Legacy / Other (9 种)
- [ ] Python
- [ ] HTML/CSS/JavaScript
- [ ] React
- [ ] Vue.js
- [ ] iOS 应用开发
- [ ] Android 应用开发
- [ ] 微信小程序
- [ ] Chrome 插件
- [ ] 通用开发规则

## 常见问题排查

### 问题 1：插件没有激活
**症状**：命令面板找不到 "HT: 添加 Cursor 规则文件"

**解决方案**：
1. 检查 `package.json` 中的 `activationEvents`
2. 查看调试控制台是否有错误
3. 确认编译成功（`npm run compile`）

### 问题 2：规则文件找不到
**症状**：提示 "找不到规则模板文件"

**解决方案**：
1. 确认 `rules/` 目录下有对应的 `.cursorrules` 文件
2. 检查 `fileName` 是否正确
3. 查看文件路径是否正确

### 问题 3：类别不显示
**症状**：选择类别时没有显示任何选项

**解决方案**：
1. 检查 `CATEGORIES` 数组是否正确
2. 确认 `showCategorySelector` 函数执行
3. 查看是否有工作区打开

### 问题 4：规则不显示
**症状**：选择类别后没有显示规则

**解决方案**：
1. 检查 `RULE_TEMPLATES` 中的 `category` 字段
2. 确认过滤逻辑正确
3. 查看是否有规则属于该类别

## 开发环境配置

### 必需软件
- Node.js (v18+)
- VSCode (v1.93.0+)
- TypeScript (v5.1.6+)

### 依赖安装
```bash
npm install
```

### 编译命令
```bash
# 一次性编译
npm run compile

# 监听模式（自动编译）
npm run watch
```

### 打包命令
```bash
# 生成 .vsix 文件
npm run package
```

## 调试配置文件

### .vscode/launch.json
```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "运行扩展",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}
```

### .vscode/tasks.json
```json
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "npm",
            "script": "watch",
            "problemMatcher": "$tsc-watch",
            "isBackground": true,
            "presentation": {
                "reveal": "never"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
```

## 贡献代码

### 添加新规则

1. **下载规则文件**
   ```bash
   cd rules
   curl -o "新规则.cursorrules" "规则URL"
   ```

2. **更新 extension.ts**
   在 `RULE_TEMPLATES` 数组中添加：
   ```typescript
   {
       id: '规则ID',
       name: '规则名称',
       description: '规则描述',
       category: '类别ID', // frontend, backend, mobile, testing, language, legacy
       fileName: '新规则.cursorrules'
   }
   ```

3. **测试规则**
   - 编译代码
   - 启动调试
   - 测试新规则是否正常工作

4. **更新文档**
   - 更新 README.md
   - 更新 CHANGELOG.md

### 提交 Pull Request

1. Fork 项目
2. 创建特性分支
3. 提交代码
4. 创建 Pull Request

---

**祝调试顺利！🚀**

